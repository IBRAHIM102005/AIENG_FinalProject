# Dependencies needed to RUN the provided ai/ package.
# Add your own SE-layer dependencies (tenacity, asyncpg, fastapi/click, etc.)
# to your project's main requirements.txt.

httpx>=0.27         # async HTTP for the source fetchers
numpy>=1.24         # imported by the shared provider base classes
pydantic>=2.7

# Pick ONE or more LLM providers depending on what you'll use:
anthropic>=0.39.0      ; extra == "anthropic"
openai>=1.50.0         ; extra == "openai"
google-genai>=0.3.0    ; extra == "gemini"

# Pick ONE web search provider:
# Tavily and Serper are accessed via plain httpx (already required) — no extra package.
duckduckgo-search>=6.0 ; extra == "duckduckgo"

# Smoke tests
pytest>=8.0
pytest-asyncio>=0.23

# ---------------------------------------------------------------------------
# Async HTTP (source fetchers + shared connection pool)
# ---------------------------------------------------------------------------
httpx==0.28.1

# ---------------------------------------------------------------------------
# Data validation
# ---------------------------------------------------------------------------
pydantic==2.11.4
pydantic-settings==2.9.1      # typed config from .env (Üzv A: src/config.py)

# ---------------------------------------------------------------------------
# Configuration — .env loading
# ---------------------------------------------------------------------------
python-dotenv==1.1.0

# ---------------------------------------------------------------------------
# CLI (Üzv B: src/cli.py)
# ---------------------------------------------------------------------------
click==8.1.8

# ---------------------------------------------------------------------------
# Retries with exponential back-off (src/services/ai_service.py, orchestrator)
# ---------------------------------------------------------------------------
tenacity==9.1.2

# ---------------------------------------------------------------------------
# NumPy — imported by the shared provider base classes (ai/providers/base.py)
# ---------------------------------------------------------------------------
numpy==2.2.5

# ---------------------------------------------------------------------------
# LLM provider SDKs
# Install the one(s) you configure in .env.
# Comment out providers you are not using to keep the image lean.
# ---------------------------------------------------------------------------
anthropic==0.52.0
# openai==1.82.0
google-genai==1.14.0

# ---------------------------------------------------------------------------
# Web search providers
# Only one is needed at runtime; Tavily and Serper use plain httpx (above).
# ---------------------------------------------------------------------------
# duckduckgo-search==7.5.3    # only needed if WEB_SEARCH_PROVIDER=duckduckgo

# ---------------------------------------------------------------------------
# Storage
# sqlite3 is bundled with Python 3.12; no extra package needed.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.0
pytest-mock==3.14.0

# ---------------------------------------------------------------------------
# Async HTTP mocking for test_services.py and test_end_to_end.py (Üzv B)
# ---------------------------------------------------------------------------
respx==0.22.0
pytest-httpx==0.35.0

# ---------------------------------------------------------------------------
# Code quality (CI only — not needed at runtime)
# ---------------------------------------------------------------------------
# ruff==0.11.10
# mypy==1.15.0
